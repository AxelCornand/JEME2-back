{% extends 'base.html.twig' %}

{% block title %}SubCategory{% endblock %}

{% block body %}
<div class="container">
    <h1>SubCategory</h1>
    <a href="{{ path('app_sub_category_index') }}" class="btn btn-sm btn-primary">back to list</a>

    <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}" class="btn btn-sm btn-warning">edit</a>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_category.name }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('sub_category/_delete_form.html.twig') }}
</div>
{% endblock %}
