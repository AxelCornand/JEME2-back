{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="container">
    <h1>Category</h1>
    <a href="{{ path('app_category_index') }}" class="btn btn-sm btn-primary">back to list</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="btn btn-sm btn-warning">edit</a>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('category/_delete_form.html.twig') }}
</div>
{% endblock %}
